# Foundation Scope

Foundation proves repository structure, build/tooling, schema/migrations, RLS/RPC, auth skeleton, seed/importer mechanics, storage boundaries, testing, CI/security gates, environment isolation, backup/restore automation and runbooks.

It does not implement final MVP feature behavior or final UI. Skeleton routes/forms exist only where required to prove auth, routing, build and trust boundaries.
