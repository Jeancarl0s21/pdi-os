name: Format Foundation lockfile

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  format-lockfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: foundation/bootstrap
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Format lockfile
        run: pnpm dlx prettier@3.9.6 --write pnpm-lock.yaml
      - name: Commit formatted lockfile
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pnpm-lock.yaml
          git diff --cached --quiet && { echo 'Lockfile already formatted'; exit 0; }
          git commit -m 'style: format Foundation lockfile'
          git push origin HEAD:foundation/bootstrap
